apiVersion: v1
kind: Service
metadata:
  name: order-service
  labels:
    app: order-service
    app.kubernetes.io/part-of: sstore

spec:
  type: ClusterIP

  selector:
    app: order-service

  ports:
    - name: http
      port: 8083
      targetPort: http