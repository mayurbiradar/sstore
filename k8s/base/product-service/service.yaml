apiVersion: v1
kind: Service
metadata:
  name: product-service
  labels:
    app: product-service
    app.kubernetes.io/part-of: sstore

spec:
  type: ClusterIP

  selector:
    app: product-service

  ports:
    - name: http
      port: 8082
      targetPort: http